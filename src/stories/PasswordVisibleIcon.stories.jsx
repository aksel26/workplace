import PasswordVisibleIcon from "../components/UI/atoms/PasswordVisibleIcon";

export default {
  title: "atoms/PasswordVisibleIcon",
  component: PasswordVisibleIcon,
};

const Template = (args) => <PasswordVisibleIcon {...args} />;

export const Default = Template.bind({});
// Default.args = {
//   checked: false,
// };
